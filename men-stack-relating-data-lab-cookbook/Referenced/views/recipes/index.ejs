<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Recipes</title>
</head>

<body>
    <%- include('../partials/_navbar.ejs')%>

        <h1>Welcome to your Recipes <%= user.username%>
        </h1>
        <ul>
            <% recipes.forEach(recipe => { %>
                <li>
                    <h3>
                        <%= recipe.name %>
                    </h3>
                    <a href="/users/<%= user._id %>/recipes/<%= recipe._id%>/details">
                        <h3>
                            Details
                        </h3>
                    </a>
                </li>
<br>
                <a href="/users/<%= user._id %>/recipes/<%= recipe._id%>/edit">edit</a>

                <form action="/users/<%= user._id %>/recipes/<%= recipe._id%>?_method=DELETE" method="POST">
                    <button>delete</button>
                </form>

                <br><br>
                <% }) %>
        </ul>
        <a href="recipes/new">Add New Recipe</a>
</body>

</html>